# AI 代码风格与架构约束


## 守卫优先硬规则


AI 每次 Edit/Write/MultiEdit 后，必须先读取并处理 PostToolUse hook 输出，再继续任何测试、验证、解释、总结或下一步修改。


出现以下任一情况时，下一步只能处理守卫问题，不得继续需求：


- `hook blocking error`
- `Style guard notice` 中的 `警告:`
- `Design hook findings requiring review`
- hook 脚本路径错误、执行失败、超时
- touched file / touched dir 违反本文件规则


处理每条 hook 输出时必须明确分类：


- `已修复`
- `旧债未扩大`
- `误报，原因是 ...`
- `阻塞，原因是 ...`


旧代码超限只允许标记为 `旧债未扩大`，前提是本次没有新增文件、没有增加超限文件行数、没有扩大同类问题。


最终回复前必须确认：


- 没有未处理的 hook blocking error
- 没有未处理的 design hook finding
- 本次触碰文件的 style guard 已成功运行


本文件约束 AI 在本项目中的所有代码新增和修改。AI 每次新增或修改代码后，只要hook/守卫提示本次触碰文件、触碰目录或修改内容违反本文件任一规则，下一步必须优先修正该违规，直至同类提示消失，再继续后续需求；若修改前未违规，修改后不得新增违规。修正范围以 hook/守卫提示的触碰文件或触碰目录为边界，可连带调整该范围内为达标所必需的旧代码及引用关系，但不得借机做与本次违规无关的全仓重构。


1. 使用规范、简洁的中文注释；只解释业务原因、约束、例外，不解释显而易见的代码。
2. 环境变量、数据库连接、外部服务地址、端口、密码、密钥、token、服务器地址必须放在配置文件或环境变量中，禁止硬编码在业务代码里。
3. 单个源代码文件目标小于 500 行；单个文件夹源代码数量目标小于 10 个。旧代码超限只提示，新代码不得继续扩大问题。
4. 代码中不允许出现未使用的变量、导入、函数、方法等。
5. 关键业务操作、异常、外部调用需记录结构化日志，并包含请求追踪 ID。
6. 异步任务应使用消息队列、事件驱动或项目已有调度器，避免长时间阻塞请求线程。
7. 对外暴露 API 遵循 RESTful 设计，使用标准 HTTP 方法和状态码。
8. 数据库访问使用 Repository 模式；内部优先使用 ORM。若项目当前使用原生 SQL，新增 SQL 必须参数化，特殊原生 SQL 必须用中文注释说明原因。
9. 异常应向上抛到全局错误处理器，返回标准化错误响应格式；禁止直接把 `str(e)` 作为响应返回给前端。
10. 核心业务逻辑与基础设施（数据库、缓存、消息队列、第三方 API）解耦，优先使用依赖注入。
11. 模块按功能垂直划分，内部高内聚；模块间通过接口或抽象类通信，避免直接耦合。
12. 模块间优先通过事件或消息通信，传递 DTO，不暴露内部实体或数据库 row。
13. 隔离外部系统（数据库、第三方 API、文件系统、邮件、WebSocket 等），使用适配器、服务或仓储模式封装。
14. `.env` 放敏感信息（密码、密钥、服务器地址等）；`config.py` 或项目配置文件放固定参数（UI 配置、业务规则、默认路径等）；敏感配置必须提供 `.env.example` 示例文件，示例值不得包含真实密钥。真实 `.env` 只允许追加用户明确提供的值，不得在回复、日志或提交信息中暴露。
